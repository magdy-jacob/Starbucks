1. **You**: Refactor this code and tell me what did you change: 
```html
item.forEach (link => {
            link.addEventListener("click" , (e) =>{
                indicater(e.target); 
            })
        })
```